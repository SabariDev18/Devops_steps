apiVersion: v1
kind: Pod
metadata:
 name: podconfig
spec:
 nodeName: ip-172-31-6-124.ap-south-1.compute.internal
 containers:
  - name: config1
    image: alpine
    command: ["sleep","1000"]
    env:
    - name: file
      valueFrom:
       configMapKeyRef:
         name: configm
         key: file
    - name: next
      valueFrom:
       configMapKeyRef:
         name: configm
         key: next

